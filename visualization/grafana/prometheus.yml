global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'iwsn_node'
    static_configs:
      - targets: ['node_exporter:9100']
    # Picks up /tmp/iwsn_metrics.prom via textfile collector
