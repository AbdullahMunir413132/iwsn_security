/*
 * ══════════════════════════════════════════════════════════════
 *  IWSN SECURITY — WiFi & MQTT Credentials
 * ══════════════════════════════════════════════════════════════
 *
 *  INSTRUCTIONS:
 *    1. Copy this file: cp credentials.h.example credentials.h
 *    2. Edit credentials.h with your actual network details
 *    3. credentials.h is gitignored — never commit real credentials
 *
 * ══════════════════════════════════════════════════════════════
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// ── WiFi Configuration ──
#define WIFI_SSID       "OnePlus 8"
#define WIFI_PASSWORD   "69000069"

// ── MQTT Broker (current host IP) ──
#define MQTT_BROKER_IP  "10.7.88.12"
#define MQTT_PORT       1883

// ── Node Identity ──
// Change this for each ESP32 node in the IWSN (node_01, node_02, etc.)
#define NODE_ID         "esp32_iwsn_node_01"

#endif // CREDENTIALS_H
